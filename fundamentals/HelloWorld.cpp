//first cpp helloworld program
#include <iostream>

using namespace std; //ensures we don't have to call std::cout, and 
                     //re-call for each function in the 
                     //standard library.
int main(){
    cout << "Hello, World!" << endl; 
    return 0;
}

//running from cmd, "gcc+ HelloWorld.cpp -o HelloWorld

//this creates HelloWorld files which can be
//executed by ./HelloWorld when we are in the directory
//of that file.
